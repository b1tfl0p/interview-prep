class Solution:
    """
    Use an adjacency graph with a custom struct
    to disambiguate between same letters at different positions
    """
    def findWords(self, board: list[list[str]], words: list[str]) -> list[str]:

